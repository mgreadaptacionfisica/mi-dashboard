// Fallback estático de los pasos de cada proyecto. Vacío por lo mismo que
// data/proyectos.js: la sección nace ya con su tabla en Supabase.
//
// Forma de cada registro:
// { id: string, proyectoId: string, texto: string, hecho: boolean,
//   prioridad: 'alta'|'media'|'baja'|null, fecha: string|null, orden: number }
export default []
